#include "main.h"

/**
 * main - function that test if function is positive or negative
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	i=0;
	positive_or_negative(i);

	return (0);
}
